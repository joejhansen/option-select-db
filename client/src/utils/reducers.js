export default function reducer(state, action){
    switch (action.type) {
        case 'IDK':
            return {
                state
            }
        default:
            return state
    }
}