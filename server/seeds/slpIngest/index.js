const processSlp = require("./processSlp");
const processSlpBulk = require("./processSlpBulk");
const processSlpUpload = require('./processSlpUpload')

module.exports = { processSlp, processSlpBulk, processSlpUpload }